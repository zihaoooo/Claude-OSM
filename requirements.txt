# OSM -> editable SVG pipeline dependencies.
# Install:  python -m pip install -r requirements.txt
# osmnx 2.x is required (the 1.x API differs); pin to the 2 series.
osmnx>=2.1,<3
geopandas>=1.0
shapely>=2.0
numpy>=1.24

# Optional: only for quick geometry-only (non-SVG) previews via matplotlib.
# matplotlib>=3.7
